<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络与系统权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ 包可见性 -->
    <queries>
        <package android:name="com.iqoo.bbs" />
    </queries>

    <application
        android:name=".IQOOApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.IQOOLike"
        tools:targetApi="34">

        <!-- Xposed 传统兼容元数据 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 静态广播接收器：持久化接收宿主 PONG 心跳与 Token 凭据 -->
        <receiver
            android:name=".receiver.CompanionStatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="iqoobbs.action.PONG" />
                <action android:name="iqoobbs.action.RESULT" />
                <action android:name="iqoobbs.action.LOG" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
